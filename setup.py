from setuptools import setup

setup(
    name='your_package',
    version='0.1',
    install_requires=[
        'numpy',
        'scipy',
        'matplotlib',
        'pandas',
    ],
    # other setup args...
)
